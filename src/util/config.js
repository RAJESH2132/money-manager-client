const config = {
    baseUrl : import.meta.env.VITE_BASE_URL,
    cloudName : import.meta.env.VITE_CLOUDINARY_CLOUD_NAME,
    uploadPresent : import.meta.env.VITE_CLOUDINARY_UPLOAD_PRESET 
}
 export default config;